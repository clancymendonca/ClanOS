# Epoch 7 Graduation

- Loom harness sources verified (`loom_gate.py`)
- OOM shed/ack wire + mem budget enforcement (`oom_policy.rs`)
- Audit tamper chain + shadow counter (`audit_wire.rs`)
- New cap kind checklist (`NEW_CAP_KIND_CHECKLIST.md`)
- Kani tier B path via `KANI_REQUIRED` env (`kani_gate.py`)
